name: indi_games_crud
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0

  #custom dependencies
  sqflite: ^2.4.1
  sqflite_common_ffi_web:
  http:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  #Custom documents
  assets:
    - assets/documents/indi_games.json
    - assets/images/game1.jpg
